// import { fromJS } from 'immutable';
// import { selectEntitiesDomain } from '../selectors';

describe('selectEntitiesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
