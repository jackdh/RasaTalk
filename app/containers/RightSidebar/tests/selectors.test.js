// import { fromJS } from 'immutable';
// import { selectRightSidebarDomain } from '../selectors';

describe('selectRightSidebarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
