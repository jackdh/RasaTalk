// import { fromJS } from 'immutable';
// import { selectTalkFlowDomain } from '../selectors';

describe('selectTalkFlowDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
