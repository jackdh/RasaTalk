/*
 * NodeWrapper Messages
 *
 * This contains all the text for the NodeWrapper component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.NodeWrapper.header',
    defaultMessage: 'This is NodeWrapper container !',
  },
});
