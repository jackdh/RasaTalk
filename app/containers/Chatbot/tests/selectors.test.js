// import { fromJS } from 'immutable';
// import { selectChatbotDomain } from '../selectors';

describe('selectChatbotDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
