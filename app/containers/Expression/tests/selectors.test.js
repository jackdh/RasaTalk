// import { fromJS } from 'immutable';
// import { selectExpressionDomain } from '../selectors';

describe('selectExpressionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
