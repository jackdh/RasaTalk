// import { fromJS } from 'immutable';
// import { selectPermissionsDomain } from '../selectors';

describe('selectPermissionsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
