// import { fromJS } from 'immutable';
// import { selectFeedbackDomain } from '../selectors';

describe('selectFeedbackDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
