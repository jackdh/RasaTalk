// import { fromJS } from 'immutable';
// import { selectSmallTalkDomain } from '../selectors';

describe('selectSmallTalkDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
