// import { fromJS } from 'immutable';
// import { selectIntentPageDomain } from '../selectors';

describe('selectIntentPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
