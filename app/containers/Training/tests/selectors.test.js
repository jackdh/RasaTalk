// import { fromJS } from 'immutable';
// import { selectTrainingDomain } from '../selectors';

describe('selectTrainingDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
