// import { fromJS } from 'immutable';
// import { selectAgentsDomain } from '../selectors';

describe('selectAgentsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
