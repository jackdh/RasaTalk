// import { fromJS } from 'immutable';
// import { selectSynonymsDomain } from '../selectors';

describe('selectSynonymsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
